name: Web Deploy (Develop)

on:
  push:
    branches:
      - develop
    paths:
      - "apps/web/**"

permissions:
  contents: read

jobs:
  deploy-web:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate Docker Hub secrets
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
        run: |
          test -n "$DOCKERHUB_USERNAME" || { echo "Missing secret: DOCKERHUB_USERNAME"; exit 1; }
          test -n "$DOCKERHUB_TOKEN" || { echo "Missing secret: DOCKERHUB_TOKEN"; exit 1; }

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and Push Frontend Image
        env:
          WEB_IMAGE_TAG: ${{ github.sha }}
          FRONTEND_ENV_FILE: ${{ secrets.DEVELOP_FRONTEND_ENV_FILE }}
        run: |
          docker buildx build \
            --platform linux/arm64 \
            -f apps/web/Dockerfile \
            -t ${{ secrets.DEVELOP_WEB_DOCKER_IMAGE }}:${WEB_IMAGE_TAG} \
            --secret id=frontend_env,env=FRONTEND_ENV_FILE \
            --cache-from type=gha \
            --cache-to type=gha,mode=max \
            --push \
            .

      - name: Deploy Web on Develop Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.DEVELOP_SERVER_IP }}
          username: ${{ secrets.DEVELOP_SSH_USER }}
          key: ${{ secrets.DEVELOP_SSH_PRIVATE_KEY }}
          script_stop: true
          script: |
            set -eu
            cd /home/ubuntu/app

            touch .deploy.env

            sed -i "s|^WEB_DOCKER_IMAGE=.*|WEB_DOCKER_IMAGE=${{ secrets.DEVELOP_WEB_DOCKER_IMAGE }}|" .deploy.env || true
            grep -q "^WEB_DOCKER_IMAGE=" .deploy.env || echo "WEB_DOCKER_IMAGE=${{ secrets.DEVELOP_WEB_DOCKER_IMAGE }}" >> .deploy.env

            sed -i "s|^WEB_IMAGE_TAG=.*|WEB_IMAGE_TAG=${{ github.sha }}|" .deploy.env || true
            grep -q "^WEB_IMAGE_TAG=" .deploy.env || echo "WEB_IMAGE_TAG=${{ github.sha }}" >> .deploy.env

            export $(cat .deploy.env | xargs)

            docker compose pull clash-web
            docker compose up -d --no-deps clash-web
