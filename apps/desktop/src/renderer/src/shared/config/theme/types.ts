export interface Theme {
  primary: {
    normal: string
  }
  label: {
    normal: string
    strong: string
    neutral: string
    alternative: string
    assistive: string
    disable: string
  }
  line: {
    normal: string
    neutral: string
    alternative: string
  }
  fill: {
    normal: string
    neutral: string
    alternative: string
  }
  background: {
    normal: string
    neutral: string
    alternative: string
  }
}
